# Store text files with LF in the repository and check them out with the
# platform's native endings. Without this, editing on Windows turns every
# touched file into a whole-file diff.
* text=auto

*.png binary
*.jpg binary
*.svg text
